#include <iostream>

using namespace std;

void reverse(char* p, char* pre)
{
    char* next = *(p+1);
}

int main()
{
    char a[] = "woshizhu";
    reverse(a, NULL);

    cout << a << endl;

    return 0;
}
